export default function UserSection() {
  return (
    <section id="user-section">
      <ul>
      <li id="notifications" aria-label="No new notifications">🔔</li>
      <li id="login">Log In</li>
      </ul>
    </section>
  );
}
